tunnel: YOUR_TUNNEL_ID
credentials-file: /home/root1/.cloudflared/YOUR_TUNNEL_ID.json

ingress:
  - hostname: pollination.your-domain.com
    service: http://localhost:5000
  - service: http_status:404
